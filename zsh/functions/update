# パッケージを更新・アップグレード（brew）
# brew doctor は毎回実行すると遅く警告だけで非ゼロ終了するため既定から外した（#28）。
# 診断したいときは明示的に `brew doctor` を叩く。
update() {
	brew update && brew upgrade && brew cleanup \
		&& echo "Homebrew packages updated."
}
