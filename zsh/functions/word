# Word で開く（open -a）
word() {
	local filepath="$1"
	local template="${DOTFILES:-$HOME/dotfiles}/templates/template.docx"

	if [ -z "$filepath" ]; then
		echo "Usage: word [path]"
		return 1
	fi

	[[ "$filepath" != *.docx ]] && filepath="${filepath}.docx"
	[ ! -f "$filepath" ] && cp "$template" "$filepath"

	open -a "Microsoft Word" "$filepath"
}
